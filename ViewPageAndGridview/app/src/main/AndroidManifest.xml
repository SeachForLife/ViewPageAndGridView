<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.traciing.corecode" >

    <!-- 获取手机状态权限 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- 自动升级权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="com.traciing.TraciingApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo_carl"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name=".ViewPageActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MaterManagerActivity"
            android:label="@string/title_activity_mater_manager" >
        </activity>
        <activity
            android:name=".MonitoredActivity"
            android:label="@string/title_activity_monitored" >
        </activity>
    </application>

</manifest>
